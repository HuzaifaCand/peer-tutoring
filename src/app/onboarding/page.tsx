import RoleSelection from "@/components/users/RoleSelection";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Onboarding | PeerLink",
};

export default function RoleSelectionPage() {
  <RoleSelection />;
}
